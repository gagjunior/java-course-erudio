server:
  port: 8765

#Spring
spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
    
#Eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
